[package]
name = "handler-chain"
version = "0.1.0"
edition = "2021"

[dependencies]
dbot-core = { path = "../../dbot-core" }
async-trait = "0.1"
anyhow = "1.0"
tracing = "0.1"
chrono = "0.4"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

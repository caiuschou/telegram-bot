# Telegram Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token_here

# Database URL (SQLite file or PostgreSQL/MySQL connection string)
DATABASE_URL=file:./telegram_bot.db

# Memory: store type (memory | sqlite | lance), optional SQLite for recent messages only
# MEMORY_STORE_TYPE=memory
# MEMORY_SQLITE_PATH=./data/memory.db
# When MEMORY_RECENT_USE_SQLITE=1: recent messages (RecentMessagesStrategy) use SQLite; semantic search still uses MEMORY_STORE_TYPE (e.g. lance)
# MEMORY_RECENT_USE_SQLITE=0

# RAG context: recent messages limit (default: 10), semantic search top-k (default: 5), min similarity score (default: 0.0 = no filter)
# MEMORY_RECENT_LIMIT=10
# MEMORY_RELEVANT_TOP_K=5
# MEMORY_SEMANTIC_MIN_SCORE=0.0

# Embedding provider for RAG semantic search: openai | zhipuai (default: openai)
# EMBEDDING_PROVIDER=openai

# BigModel / Zhipu AI API key (required when EMBEDDING_PROVIDER=zhipuai)
# BIGMODEL_API_KEY=your_bigmodel_key
# ZHIPUAI_API_KEY=your_zhipu_key

# AI 系统提示词（人设/行为）；不设置则使用内置默认。可多行，内容会作为 OpenAI system 消息。
# AI_SYSTEM_PROMPT=你是一个讲话很嗲的台湾女生...

# 流式回复时，两次编辑同一条消息的最小间隔（秒），用于控制 Telegram 编辑请求频率，默认 5。
# TELEGRAM_EDIT_INTERVAL_SECS=5

# Log Level (optional: trace, debug, info, warn, error).
# teloxide/reqwest/hyper are forced to warn to reduce framework debug noise.
# To enable teloxide debug: RUST_LOG=debug,teloxide=debug
# RUST_LOG=info

# OpenAI API Configuration
# Required for ReAct chat functionality

# OpenAI API Key (required)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model (optional, defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# When -m/--messages is not set: load uses Telegram DB if this is set, else LANGGRAPH_MESSAGES_PATH (JSON).
# Path to Telegram bot SQLite (messages table). thread_id is then used as chat_id.
# TELEGRAM_MESSAGES_DB=./data/telegram.db

# Load from JSON when -m not set and TELEGRAM_MESSAGES_DB is not set
# LANGGRAPH_MESSAGES_PATH=messages.json

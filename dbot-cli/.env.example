# ============================================
# Dbot CLI 配置文件
# ============================================

# Telegram Bot Token（从 @BotFather 获取）
BOT_TOKEN=你的_bot_token_请替换为真实token

# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# 词向量配置（dbot load 时使用）
# 服务: openai / zhipuai（智谱）
EMBEDDING_PROVIDER=openai
# 词向量模型（可选，不填则用各服务默认）
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_MODEL=embedding-2
# 智谱 API Key（EMBEDDING_PROVIDER=zhipuai 时必填）
# BIGMODEL_API_KEY=your_bigmodel_key

# 模型配置（对话模型，与词向量无关）
MODEL=gpt-3.5-turbo

# 流式响应配置（可选）
USE_STREAMING=false
THINKING_MESSAGE=正在思考...

# 数据库配置（可选）
DATABASE_URL=file:./telegram_bot.db

# 记忆存储配置（可选）
# 存储类型: memory（内存）, sqlite（SQLite）, lance（LanceDB）
MEMORY_STORE_TYPE=memory
# LanceDB 数据库路径（当 MEMORY_STORE_TYPE=lance 时使用）
LANCE_DB_PATH=./data/lancedb
# SQLite 记忆数据库路径（当 MEMORY_STORE_TYPE=sqlite 时使用）
MEMORY_SQLITE_PATH=./data/memory.db

# 速率限制配置（可选）
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_SECS=60

# 日志级别 (可选: trace, debug, info, warn, error)
RUST_LOG=info

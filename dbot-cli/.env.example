# ============================================
# Dbot CLI configuration
# ============================================

# Telegram Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token_replace_with_real_token

# OpenAI configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# Embedding config (used by dbot load)
# Provider: openai / zhipuai (Zhipu)
EMBEDDING_PROVIDER=openai
# Embedding model (optional; leave empty to use provider default)
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_MODEL=embedding-2
# Zhipu API Key (required when EMBEDDING_PROVIDER=zhipuai)
# BIGMODEL_API_KEY=your_bigmodel_key

# Model config (chat model, unrelated to embedding)
MODEL=gpt-3.5-turbo

# Streaming response (optional)
USE_STREAMING=false
THINKING_MESSAGE=Thinking...

# Database (optional)
DATABASE_URL=file:./telegram_bot.db

# Memory store (optional)
# Store type: memory (in-memory), sqlite (SQLite), lance (LanceDB)
MEMORY_STORE_TYPE=memory
# LanceDB path (when MEMORY_STORE_TYPE=lance)
LANCE_DB_PATH=./data/lancedb
# SQLite memory DB path (when MEMORY_STORE_TYPE=sqlite)
MEMORY_SQLITE_PATH=./data/memory.db

# Rate limit (optional)
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_SECS=60

# Log level (optional: trace, debug, info, warn, error)
RUST_LOG=info
